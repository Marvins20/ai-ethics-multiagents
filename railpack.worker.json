{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "uv sync"
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": []
    }
  },
  "deploy": {
    "startCommand": "uv run arq src.worker.settings.WorkerSettings"
  }
}
